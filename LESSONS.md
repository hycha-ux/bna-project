- **셸 히어독은 백슬래시를 한 겹 먹는다 — 소스는 Write/Edit로 써라.** `<<'EOF'`로 따옴표를 걸었는데도 `'C:\Users\...'`가 `'C:\Users\...'`로 저장돼 JS가 `\U`를 이스케이프로 먹었다(`C:Usersmedib`). 오류는 나중에 엉뚱한 자리에서 났다 → 경로는 슬래시로 쓰고, 소스 파일은 히어독으로 만들지 마라.
