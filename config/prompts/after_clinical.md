Edit this exact photo. {identity_lock}
{retake}
{micro_drift}
Only change: {after_change}
Preserve all skin texture (pores, peach fuzz, small blemishes) outside the treated area. Do not brighten, smooth, or re-render anything else.
{avoid}
